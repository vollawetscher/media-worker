app = "livekit-media-worker"
primary_region = "iad"

[build]

[env]
  MODE = "both"
  POLLING_INTERVAL_MS = "3000"
  HEARTBEAT_INTERVAL_MS = "15000"
  LOG_LEVEL = "info"

[[vm]]
  size = "shared-cpu-1x"
